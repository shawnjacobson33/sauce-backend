services:
  db:
    image: mongo
    container_name: mongo-instance
    ports:
      - "27017:27017"

  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: app-instance
    ports:
        - "8000:8000"
    depends_on:
        - db
    volumes:
        - ./app:/app
